<%= form_with(model: fator, local: true) do |form| %>
  <% if fator.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(fator.errors.count, "error") %> prohibited this fator from being saved:</h2>

      <ul>
      <% fator.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
