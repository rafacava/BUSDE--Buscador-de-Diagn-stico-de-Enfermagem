<h1>Editing Diagnostico</h1>

<% if current_user && current_user.admin %>
<%= render 'form', diagnostico: @diagnostico %>
<%= link_to 'Show', @diagnostico %> |
<% else %>
<h1>ACESSO NÃO AUTORIZADO</h1>
<% end %>
<%= link_to 'Back', diagnosticos_path %>
